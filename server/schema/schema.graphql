type RecipeIngredient {
  id: ID
  quantity: String!
  unit: String!
  name: String!
  note: String
}

type RecipeStep {
  id: ID
  body: String!
}

type Recipe {
  id: ID
  title: String!
  # numServings: Int!
  # prepTimeMinutes: Int!
  # cookTimeMinutes: Int!
  # totalTimeMinutes: Int!
  # ingredients: [RecipeIngredient!]!
  steps: [RecipeStep!]!
  # notes: [String!]
  # tags: [String!]
}

type Query {
  recipes: [Recipe!]
}
